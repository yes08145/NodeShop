<!DOCTYPE html>
 <!--  Content (배너이미지) 부분 -->
  <div> <img src="/images/auctionbanner.jpg" class="img-fluid" alt="배너이미지" width="100%"> </div>

  <!-- 컨텐츠(예, 쇼핑몰일경우 판매제품리스트)헤더부분 -->
   <div class="container-fluid" style="border: solid 20px white; border-radius: 1px">
     <div class="panel panel-default">
   	  <div class="panel-heading">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item">관리자></li>
            <li class="breadcrumb-item active" aria-current="page">거래 데이터</li>
          </ol>
        </nav>
   	  </div>

      <hr>
      <div class="container">

          <table class="table table-striped">
            <thead>
              <tr>
                <th> 구분번호 </th>
                <th> 구매자 </th>
                <th> 판매자 </th>
                <th> 상품 이름 </th>
                <th> 상품 가격 </th>
                <th> 배송 상태 </th>
                <th> 거래 시간 </th>
                <th>
              </tr>
            </thead>
            <tbody>
               <% var total = 0; %>
               <% for(var i=(page*page_num)-page_num; i<(page*page_num); i++){
                 if(i>length){
                   i++;
                 }else{
                   var item = tradedata[i];
               %>
              <tr>
                <td> <%= item.result_num %> </td>
                <td> <%= item.pur_id %> </td>
                <td> <%= item.sell_id %> </td>
                <td> <%= item.p_name %> </td>
                <td> <%= item.p_price %> </td>
                <td> <%= item.delivery %> </td>
                <td> <%= item.date.toLocaleDateString(); %> </td>
              </tr>
            <% }} %>
            </table>
          </div>

  <p>
  <nav aria-label="...">
    <ul class="pagination justify-content-center">
      <li class="page-item">
        <a class="page-link" href="/trade/list/<%= previous %>" tabindex="-1" aria-disabled="true">이전</a>
      </li>
      <% for(var j = 0; j < length / page_num; j++){  %>
        <li class="page-item"><a class="page-link" href="/trade/list/<%= j + 1 %>"><%= j + 1 %></a></li>
      <% } %>
      <li class="page-item">
        <a class="page-link" href="/trade/list/<%= next %>">다음</a>
      </li>
    </ul>
</nav>
